/*Faça um programa em que troque todas as ocorrências de uma letra L1
pela letra L2 em uma string. A string e as letras L1 e L2 devem ser fornecidas pelo
usuário.*/

#include<stdio.h>
#include<string.h>
#include<ctype.h>

int main(){

    char string[20];
    char l1,l2;

    printf("Entre com a uma string:\n");
    gets(string);

    printf("Entre com a letra L1(retirar da frase):\n" );
    scanf("%c",&l1);

    fflush(stdin);
    printf("Entre com a letra L2(colocar no lugar da l1):\n");
    scanf("%c",&l2);

    for(int i=0; i<20; i++){
        if(string[i]==l1){
            string[i]=l2;
        }
    }

    puts(string);

    return 0;
}
